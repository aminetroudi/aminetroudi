# Mohamed Amine Troudi

Software engineer, eleven years in production systems. Strasbourg, France.

I build software I am passionate about, and the systems it runs on. Lately that
means around 30 self-hosted services, split between a home lab and the cloud, a
private mesh network tying them together, and local language models running on
my own GPUs.

### What I work with

Linux on bare metal, Ansible, Docker, Traefik. Java and Spring Boot, Node,
Python. Angular and Vue. Prometheus and Grafana when things need watching.
FFmpeg, RTMP and SRT when the payload is video.

### What is here

Personal projects, built outside work.

**claudemap** : a local dashboard for a Claude Code setup, showing skills,
agents, plugins, MCP servers, memory and live sessions.

**quotesjs** : a quotes API with per-key quotas, API keys and Google sign-in,
plus a web front end.

**hadith-daily** : a daily hadith app for Android and iOS, with a native
home-screen widget and a curation pipeline behind it.

**pray-time-calculator** : prayer times and the Hijri calendar as a library,
with a web interface on top. Works offline, no third-party API.

### Elsewhere

[troudi.dev](https://troudi.dev)

Arabic, French, English.
